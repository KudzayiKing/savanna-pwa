# 2026-09-08

## Request 13 — animate gold/white megaphone logo + replace gold/black logo everywhere

User added `savanna_megaphone.svg` (gold `#DDAC2C` + white, no dark pixels;
raster 890×890 wrapped in SVG via `<use x="72" y="72">`). Task: animate the
sound waves exactly like the old logo and replace `savanna_new_logo.svg`
(gold/black) on the splash, PWA app icon, and web sidebar rail.

### What shipped
- `client/public/savanna_megaphone.svg` (597,804 B) — animated. Three white
  tapered-crescent waves measured from the raster (NOT arc-fit — they're
  crescents, IoU-vs-arc poor) via connected-components + marching-squares +
  RDP. Cover pass in gold `#DDAC2C` then white fill on top. Choreography:
  inner→mid→outer fade IN, then outer→mid→inner fade OUT, 2.6s loop.
  `prefers-reduced-motion` → static. No overflow (5px halo around waves
  touches only gold).
- `client/public/savanna_megaphone_icon.svg` (60,775 B) — static rail icon.
  Embedded 256 raster but declared `width="890" height="890"` + `<use x="72"
  y="72"/>` so framing matches the animated logo for a clean hover cross-fade.
- Regenerated `client/public/icons/*` on a `#131213` dark tile (composited
  from the new logo raster): `icon-192.png`, `icon-512.png`,
  `icon-maskable-512.png`, `apple-touch-icon.png`, `favicon-32.png`,
  `icon.svg`, `icon-maskable.svg`. Corners `(19,18,19,255)`, centre white.
- Wiring: `client/index.html:150` splash → `/savanna_megaphone.svg`;
  `client/src/components/SavannaShell.tsx:114,119` rail two-layer cross-fade;
  `client/public/service-worker.js` `CACHE_NAME="savanna-shell-v29"` +
  `SHELL_URLS` lists both new SVGs; `client/src/main.tsx:75`
  `WORKER_URL="...?v=29"`; `server/pwa.assets.test.ts` assertions for the
  above strings updated (NOT run — standing "do not run tests" instruction).
- Deleted orphans: `savanna_new_logo.svg`, `savanna_new_logo_icon.svg`
  (old gold/black), `savanna_megphone.svg` (typo-named source). Left
  untouched: `savanna_logo.svg`, `savanna_megaphone_vector.svg` (unreferenced).

### Verification
- Animated vs original raster = **0 differing pixels** (cover swap invisible).
- Headless-Chrome frozen frames (Web Animations API) confirmed exact
  choreography at t=200/300/450/600–1400/1600/1750–1900/2050/2200–2590 ms.
- `npx tsc --noEmit` exit 0; `npx vite build` clean; `dist/public` carries
  every new asset + `savanna-shell-v29` + `?v=29` + splash/rail refs.

### Gotchas learned (added to skill `animate-raster-logo-parts`)
1. **FATAL**: a CSS comment inside an SVG `<style>` containing a literal
   `<...>` (e.g. `<svg>`, `<path>`) makes Chrome close `<style>` early,
   silently dropping `@keyframes`/`animation`. Symptom:
   `document.getAnimations().length === 0`, `animationDuration === "0s"` while
   `fill` renders fine. Fix: plain-prose comments, no angle brackets.
2. Freezing animation frames: `animation-play-state:paused` + `animation-delay`
   injection does NOT seek frames inside inline/`img` SVG. Use
   `document.getAnimations().forEach(a=>{a.currentTime=X; a.pause();})` after
   inlining the SVG into the page.
3. `pngtool.encode_png(w, h, rgba_bytes)` — arg order `(w, h, rgba)`, not
   `(rgba, w, h)`.
4. Marching-squares + RDP for ragged/non-circular raster shapes; single clean
   `key=(round(x*2),round(y*2))` adjacency, plus a bbox-sanity assert.
5. PWA icon-regen-on-dark-tile + rail-icon framing-match workflow (Step 8b).

## Request 14 — animate WHITE transparent-bg vector logo; black/white by theme; gold icon + status bar

User added `savanna_megaphone_vector.svg` (pure WHITE logo, TRANSPARENT bg,
same megaphone-in-ring artwork as R13). Animate it like R13, and:
- splash + web sidebar rail: WHITE logo recolored BLACK in light theme, WHITE
  in dark theme (single white source, recolored via CSS `filter`).
- PWA desktop app icon + notification/status-bar accent: brand GOLD `#DDAC2C`.

### Decisions (AskUserQuestion)
- Gold scope: "Live + installed" → `#DDAC2C` set as `theme_color` everywhere
  (in-app + installed status bar + notification accent).
- Recolor: "CSS filter on one file" → one white animated SVG; light theme
  `filter: brightness(0)` (→black), dark theme no filter (→white). No dupes.
- Gold hex: `#DDAC2C` (the logo's existing gold).

### What shipped
- `client/public/savanna_megaphone_vector.svg` (527,741 B) — animated, white
  transparent. Same 3-wave choreography as R13 (inner→mid→outer fade IN, then
  reverse, 2.6s loop, reduced-motion → static). The 3 waves are isolated from
  the raster via connected-components, traced (Moore-Neighbor boundary trace +
  1px dilation + RDP eps=2.0) into `<path>` overlay; the base raster has those
  pixels MASKED OUT (body-only) so the overlay reconstructs the full logo.
  Verified pixel-perfect: anim@t0 vs body-only = 0 diff, anim@t1000 vs full =
  0 diff.
- `client/public/savanna_megaphone_icon.svg` (526,110 B) — white static rail
  icon (full raster, same `<use x="72" y="72">` framing).
- `client/public/icons/*` REGENERATED on a GOLD `#DDAC2C` tile (was dark
  `#131213` in R13): icon-192/512, icon-maskable-512, apple-touch, favicon-32,
  icon.svg, icon-maskable.svg. Corners `(221,172,44)`=#DDAC2C, centre white.
- Wiring: `index.html:150` splash → vector; added
  `html:not(.dark) #splash .savanna-splash-mark > img { filter: brightness(0); }`
  (light → black); inline-script `theme-color` → `#DDAC2C` + status-bar
  `default`; `SavannaShell.tsx:114,119` rail two-layer cross-fade, both imgs
  got class `savanna-rail-mark`; `index.css` added
  `.savanna-rail-mark { filter: brightness(0); }` +
  `.dark .savanna-rail-mark { filter: none; }` (light→black, dark→white);
  `service-worker.js` `CACHE_NAME="savanna-shell-v30"` + vector in SHELL_URLS;
  `main.tsx:75` `WORKER_URL="...?v=30"`; all 3 manifests `theme_color` →
  `#DDAC2C`; `pwa.assets.test.ts` theme_color×3 + CACHE_NAME + worker/WORKER_URL
  + `html`/`shell` `src` assertions updated (NOT run — standing instruction).
- Orphan `client/public/savanna_megaphone.svg` (gold/white, R13 output, now
  unreferenced) moved to `/tmp/savanna_megaphone_orphan_goldwhite.svg` (kept,
  not hard-deleted).

### Verification
- `npx tsc --noEmit` exit 0; `npx vite build` clean; `dist/public` carries
  vector (527,741 B, identical to client/public), gold icons,
  `savanna-shell-v30`, `?v=30`, splash `brightness(0)` + vector src,
  `theme-color #DDAC2C` ×3 manifests, rail `savanna-rail-mark` ×2 + vector.
- Structural: 3 wave paths + 3 keyframes + `prefers-reduced-motion` + fill
  `#FFFFFF` all present; client vs dist byte-identical.

### Gotchas learned (added to skill `animate-raster-logo-parts`)
6. **Transparent-bg logo: you can't "cover" with a bg colour.** Mask the
   target pixels to transparent in the base raster (body-only) and overlay the
   traced vector. Dilate the mask 1px so the overlay fully covers the original.
7. **FATAL re-run trap:** a build/trace script that writes its OUTPUT to the
   same path as the SOURCE overwrites (and destroys) the user's original. This
   session overwrote `savanna_megaphone_vector.svg` on run 1; recovered from
   `dist/public/` copy. Always pin `SRC` to a read-only copy (e.g. `/tmp`) and
   write outputs to `client/public/`.

## Request 17 (BLOCKED-verify) — production stuck between splash and "Refresh Savanna"

Symptom: prod (`https://savanna-2caf0.web.app`) never mounts React; `#root` stays
empty, so `client/index.html` shows the 8s "Refresh Savanna" boot fallback.
Dev is fine. All prod assets return 200 (checked by curl), so not a 404.

**Root cause: circular chunk dependency in the production build.**
Read the import headers of `dist/public/assets/*.js`:
- `vendor-firebase-core-BrXlTnep.js` line 1: `import{o as le}from"./vendor-firebase-BY8izFKf.js"`
- `vendor-firebase-BY8izFKf.js` line 1: `import{r as se,_ as ae,…}from"./vendor-firebase-core-BrXlTnep.js"`

`o` = `openDB` from **`idb`**. `@firebase/app` (→ core chunk) imports
`openDB` (`node_modules/.pnpm/@firebase+app@0.16.1/.../dist/esm/index.esm.js:5`),
and `@firebase/installations` / `firebase/messaging` also need `idb`
→ Rollup auto-placed `idb` in `vendor-firebase` → cycle. Same failure mode as
the old `vendor` catch-all outage documented in `vite.config.ts`. Triggered by
adding web-push (`client/src/lib/firebaseNotifications.ts` → `firebase/messaging`).
Production-only because dev has no chunking.

**Fix applied (NOT verified — shell/sandbox died, see below):**
1. `vite.config.ts` `manualChunks`: `if (id.includes("/idb/") || id.includes("/tslib/")) return "vendor-firebase-core";` — first rule after the node_modules guard, with a long comment.
2. NEW `scripts/check-chunk-cycles.mjs` — DFS over emitted chunk import graph, fails on any static cycle. Wired into `build` (`vite build && node scripts/check-chunk-cycles.mjs && esbuild …`) + `check:chunks`.
3. `client/index.html` boot fallback now polls every 500ms and removes itself if `#root` gets children, so a slow late boot isn't buried under the overlay forever.

**User must run:** `npx vite build` (expect `[check:chunks] OK`) then
`/usr/local/bin/firebase deploy --only firestore:rules,firestore:indexes,hosting`.

**ENV WARNING:** running `npm install puppeteer-core` in
`~/.workbuddy-ai/binaries/node/workspace` wedged the Bash tool — every later
command (incl. `echo`) returns exit 127 with empty output, and Grep fails with
`SandboxError … sandbox-exec: Argument list too long`. Read/Write/Edit/Glob
still work. Avoid `npm install` into the managed workspace; use the project's
own pnpm store instead.

## Request 15 — status bar was gold in BOTH themes; make it match theme bg

User reported the installed-PWA status bar rendered GOLD in light AND dark
(because R14 hardcoded `theme-color` = `#DDAC2C` + forced `status-bar-style=
default`). Fix: status bar must equal the theme background color.

### Change
- `client/index.html` inline script: reverted `theme-color` from hardcoded
  `#DDAC2C` back to the already-computed `pageColor` (light `#FFFFFF`, dark
  `#0B0F0E`) and restored dark-aware `status-bar-style` = `dark ? "black" :
  "default"`. Catch block: `theme-color` → `#FFFFFF` (light fallback).
- All 3 manifests `theme_color` reverted: light/generic `#FFFFFF`, dark
  `#0B0F0E` (background, NOT gold).
- Bumped SW cache `savanna-shell-v31` + `main.tsx` `WORKER_URL="…?v=31"` +
  `pwa.assets.test.ts` CACHE_NAME/WORKER_URL/theme_color assertions (NOT run).
- `npx tsc --noEmit` + `npx vite build` clean; dist verified.

### Note for user
`theme_color` in the web manifest is shared by the Android status bar AND the
OS notification accent, so reverting it to the background color also drops the
gold notification tint (tradeoff, can't separate in a PWA manifest). Gold
remains on the PWA app icon only. If gold notifications are wanted, the status
bar would have to be gold too — offer that tradeoff.

## Request 16 — splash logo a little smaller

User: make the splash-screen logo a bit smaller (was too big); "do not run
tests". Changed `client/index.html:91` `#splash .savanna-splash-mark` width
from `min(62vmin, 320px)` → `min(54vmin, 280px)` (~13% smaller). The cache was
already at `savanna-shell-v33` (user had bumped it + added `?v=33` to the
manifest precache URLs and aligned `main.tsx`/`pwa.assets.test.ts`); no cache
bump needed from me. `npx vite build` clean; dist verified (`54vmin`/`280px`
present, old `62vmin` gone).

## Request 18 — "no push notifications on my phone" (diagnosis, not yet fixed)

Chain: `notifySavannaEvent()` (called from `firebaseChat.ts:633/730/804` and
`firebaseCommunities.ts:466/516`) writes a `notificationIntents` doc **and**
POSTs to `/api/notifications/dispatch` → Express verifies ID token → Admin
`sendEachForMulticast` → tokens from `users/{uid}/notificationDevices where
enabled == true`.

Two hard blockers found (evidence):
1. **`notificationDevices` = 0 docs in prod Firestore** while 4
   `notificationIntents` sit forever at `status:"queued"`. No FCM token has
   ever been saved → `userTokens()` returns `[]` → every send is skipped.
   User must tap **Profile → Notifications → Enable push**. (On iOS 16.4+ web
   push only works for a PWA **added to the Home Screen**, not Safari tabs.)
2. **`/api/notifications/dispatch` does not exist on Firebase Hosting.**
   `curl -X POST https://savanna-2caf0.web.app/api/notifications/dispatch`
   → `200 text/html`, 29 505 bytes of `index.html`. `firebase.json` has only
   one rewrite (`** → /index.html`). There is **no `functions/` dir at all**.
   `registerNotificationRoutes` only runs in the Express dev server and in the
   8.9 MB bundled `netlify/functions/api.mjs` — neither serves the Firebase
   host. So even with a token registered, nothing would ever send.

Verified OK (not the problem):
- `VITE_FIREBASE_VAPID_KEY` (87 chars) is **baked into `dist/public/assets`**.
- `client/public/service-worker.js` has its own `push` listener (line 291) and
  `normalizeNotificationPayload` (line 267) **does** unwrap the FCM envelope
  (`payload.notification.title` / `payload.data`), so display would work.
- No `firebase-messaging-sw.js` is needed because `getToken()` is passed an
  explicit `serviceWorkerRegistration` (`WORKER_URL = "/service-worker.js?v=37"`).

Fix options offered to user: (A) Cloud Function Firestore trigger on
`notificationIntents` — best fit, no /api, needs Blaze; (B) Cloud Function
HTTPS + `/api/**` rewrite reusing the Express route; (C) host the Netlify
function elsewhere + DNS.

Also: `Error: Failed to parse JSON file.` in the Vite dev log did **not**
reproduce (clean on a standalone Vite :5199 and on the Express dev server
:3002). All 15 JSON files outside node_modules/dist parse fine. Stack points at
`applyHtmlTransforms` ← `server/_core/vite.ts:72`. Likely transient/mid-write.

### Fix chosen + implemented (Firestore trigger)

User picked **Cloud Function Firestore trigger** and **deploy everything at
once**. New:
- `functions/package.json` (node 20 engine, `firebase-admin ^13`,
  `firebase-functions ^6.6.0` — CJS, `lib/v2/index.js`, no `"type":"module"`),
  `functions/.gitignore`, `functions/index.js`.
- `functions/index.js` exports `dispatchNotificationIntent` =
  `onDocumentCreated("notificationIntents/{intentId}")` — a line-for-line port
  of `server/_core/notificationRoutes.ts` (same validation: sender must be a
  member / own the message / own the post; same per-user settings + mute
  checks; same `sendEachForMulticast` payload). Writes `status`/`delivered`/
  `processedAt` back onto the intent doc. Never rethrows (no infinite retries).
  `ORIGIN` defaults to `https://savanna-2caf0.web.app`, `SAVANNA_ORIGIN`
  overrides, `http://127.0.0.1:3002` under the emulator.
- `firebase.json` gained a `functions` block (source `functions`, nodejs20).

Client: removed `dispatchSavannaNotification()` + its `/api/notifications/dispatch`
POST from `firebaseNotifications.ts` (that POST got 29 KB of HTML on Hosting and
threw on `.json()`). `notifySavannaEvent()` now only writes the intent doc, which
is what triggers the function. Dropped the now-unused `getFirebaseAuth` import.
`server/_core/notificationRoutes.ts` left in place for anyone running Express
locally. `npx tsc --noEmit` clean; `npx vite build` clean;
`[check:chunks] OK — 29 chunks, 180 static edges, no cycles.`

Firestore rules already correct — clients can only `create` intents with
`senderUserId == uid` and `status == 'queued'`, never read/update; the Admin SDK
bypasses rules so the function can close the loop.

**Still to do:** deploy (`firebase deploy --only
functions,firestore:rules,firestore:indexes,hosting`) — needs the Blaze plan.
Then the user must tap Profile → Notifications → Enable push so a token lands in
`notificationDevices` (currently 0). On iOS the PWA must be on the Home Screen.

## Request 19 — "enabled notifications but still nothing after deployment"

Shell had recovered. Verified with `/usr/local/bin/node` v20.17.0 + firebase
13.34.0. `require('./functions/index.js')` loads clean
(`exports: dispatchNotificationIntent, handler: function`) — the module is fine.

**Finding 1 — nothing was ever deployed as a function.**
`firebase functions:list` → empty table. All 7 intents still `status:"queued"`,
`processedAt: null`. (Hosting *was* current: local and deployed entry both
`assets/index-BFXBNm_l.js`.) `firebase deploy --only functions` fails with:
> Billing account for project '1046075850585' is not open. Billing must be
> enabled for activation of service(s) 'cloudbuild.googleapis.com,
> artifactregistry.googleapis.com, containerregistry.googleapis.com'

Cloud Functions v2 need Cloud Build + Artifact Registry, i.e. **Blaze**. There
is no way around it for a server-side FCM send — FCM HTTP v1 needs a service
account, and the legacy `key=` API was retired Jun 2024, so it cannot be done
from the browser. Alternative without Firebase billing = deploy the existing
`netlify/functions/api.mjs` (free tier) and POST cross-origin, but that needs
CORS + re-adding the client POST we just deleted.

**Finding 2 — `notificationDevices` is STILL empty (collectionGroup → NONE).**
Not even an `enabled:false` row. That means `enableSavannaNotifications()` threw
before its first `setDoc`, i.e. at `messagingSupported()` (needs
`"Notification" in window && "serviceWorker" in navigator && "PushManager" in
window && isSecureContext`, plus firebase `isSupported()`) or at
`requestPermission()`. Zero docs = zero signal, so failure was invisible.

**Fix shipped (hosting only, no billing needed):** added
`recordDeviceFailure(user, reason, detail?)` to `firebaseNotifications.ts`. It
writes `enabled:false` + `disabledReason` (`unsupported_browser`,
`missing_vapid_key`, `token_error: <msg>`, `empty_token`,
`permission_<state>`) and never throws. Also wrapped the
`ensureNotificationWorker()`+`getToken()` block in try/catch — it was the one
unguarded throw. `disabledReason` is already in the rules' `hasOnly` allowlist.
`npx tsc --noEmit` clean, `npx vite build` clean, `[check:chunks] OK`,
**84/84 vitest pass**, hosting deployed (53 files).

Next: user enables Blaze → `firebase deploy --only functions` → user taps Enable
push → read `notificationDevices.disabledReason` to see what it says.
