# 2026-09-09

## Savanna logo: chat-bubble mark + theme + splash animation
- New canonical logo: `client/public/savanna_logo_2026.svg` (chat-bubble shape with 3 gold pills).
  Old `savanna_logo.svg` / `new_savanna_logo.svg` / `savanna_megaphone_vector.svg` deleted.
- Theme contract: **dark = white bubble + gold (#D9A441) lines**; **light = near-black (#151A17) bubble + gold lines**.
  Applied in 3 places: splash (`client/index.html` inline `<style>`), rail (`client/src/index.css`
  `.savanna-rail-bubble`), and boot fallback (`client/index.html` "Refresh Savanna").
- Splash animation: `sv-line-1/2/3` wave — lines appear top→bottom then disappear bottom→top
  (one-by-one, "up and down") every 2.8s. `prefers-reduced-motion` shows static gold lines.
- GOTCHA: the logo SVG is **inlined** into the splash + rail (not an `<img>`), so its internal
  `<style>` fills do NOT apply. Must set `.savanna-bubble` and `.savanna-line` fills via CSS
  in the inlining scope (`#splash` / `.savanna-rail-bubble`). Forgetting this renders the
  bubble + lines black (SVG default) → invisible.
- Verified build via `vite build --outDir /tmp/savanna_verify` (the normal `dist/public` build is
  blocked here by the sandbox safe-delete bulk guard on emptyOutDir). Copied result into dist/public.

## Round 2 — user feedback fixes (same task)
- User deleted `client/public/savanna_megaphone_icon.svg`. The rail (`SavannaShell.tsx`) still
  referenced it as a broken `<img>`, which is what showed as the "different logo" on web. Removed
  the megaphone `<img>`; rail now shows the inlined bubble mark as the DEFAULT (white+gold dark,
  near-black+gold light). Also removed the now-unused `.savanna-rail-mark` brightness filter in
  index.css and the megaphone entry from the SW precache (bumped SW to `savanna-shell-v39`).
- Splash: user wanted ONLY the 3 lines to animate, not the whole mark. Removed `sv-mark-intro`
  + `sv-mark-breathe` from `.savanna-splash-mark` (bubble now static); the `sv-line-1/2/3` wave
  remains. Reduced-motion block no longer needs the mark reset.
- Gotcha: `cp -R` into dist/public does NOT delete stale hashed assets, so an old bundle with the
  megaphone string lingered and tripped a grep. Confirm the hash `index.html` references, not just
  any file in dist/public/assets. Removed the stale assets; dist is clean.

## Round 3 — user proved the logo geometry was wrong (screenshots)
- The inlined splash/rail SVG did NOT match the uploaded `savanna_logo_2026.svg`: the file still
  had the OLD small triangular tail (viewBox 0 0 512 512, circle (275,235) r210, tail 150,480
  175,400 215,425). User uploaded a NEW design with a large pointed tail reaching the bottom-left
  corner. Recreated the SVG at viewBox 0 0 1000 1000: circle (500,430) r350, tail polygon
  "210,620 360,730 40,960", lines x200 w600 y310/410 and x200 w380 y510. Synced splash, rail, and
  boot-fallback ("Refresh Savanna") inlines to the same geometry.
- `server/pwa.assets.test.ts` is a brittle toContain pin file: changing pinned strings breaks it.
  Updated: `sv-mark-intro` -> `sv-line-1` (only lines animate now), and `savanna-shell-v38` ->
  `savanna-shell-v39` (SW cache bump). All 25 tests pass.
